# PDF・CDF・区間確率ビジュアライザ

確率の最初の山——**「密度 $f(x)$ は確率ではない。確率は区間の面積であり、CDF の差でもある」**——を、
スライダーで動かしながら体得するための小規模ツールです。Module 2（確率変数と分布）の主教材と対で使います。

関連ノート：[`notes/02_random_variables_and_distributions.md`](../../notes/02_random_variables_and_distributions.md)（本論）、
[`notes/01_events_and_probability.md`](../../notes/01_events_and_probability.md)（確率0≠不可能）。

---

## 1. 何が体験できるか（学習目標）

1. **密度（高さ）と確率（面積）の区別**：$f(x)$ の高さは確率ではない。確率は区間の面積 $\int_a^b f$。
2. **PDF と CDF は同じ情報の別表現**：$P(a\le X\le b)=\int_a^b f = F(b)-F(a)$ を、左右2つの図で同時に見る。
3. **点の確率は0**：区間 $[x_0-\varepsilon, x_0+\varepsilon]$ を縮めると確率は0へ。高さ $f(x_0)$ は残る。
4. **超過確率＝右裾の面積**：$P(X>c)=1-F(c)$。Module 6 のチャンス制約に直結。

---

## 2. 実行方法

```bash
# リポジトリ直下で
python3 -m venv --system-site-packages .venv   # 既存の numpy/scipy/plotly を再利用
source .venv/bin/activate
pip install streamlit                            # まだなら

streamlit run apps/pdf_cdf_visualizer/app.py
```

依存：`numpy`, `scipy`, `plotly`, `streamlit`（`requirements.txt` 参照）。

数理コアだけの自己テスト（Streamlit 不要、3通りの確率計算の一致を確認）：

```bash
python3 apps/pdf_cdf_visualizer/core.py
```

---

## 3. 操作できるパラメータ

| パラメータ | 範囲・選択 | 何が変わるか |
|---|---|---|
| 分布 | 正規 / 対数正規 / 指数 / 一様 | 形（対称・右歪み・単調減少・平坦） |
| 分布パラメータ | μ, σ / μ_log, σ_log / λ / a, b | 中心・広がり・裾の重さ |
| 問い方 | 区間 / 左裾 / 右裾(超過) | 塗る領域と対応式が切り替わる |
| 区間・閾値 a, b | 台で可変 | $P(a\le X\le b)$ の積分範囲 |
| 点 $x_0$, 半幅 $\varepsilon$ | 台で可変 / 離散 | 「密度≠確率」実験の点と微小区間 |

---

## 4. 観察すべき点

- 正規で **σ を大きく** → 山は低く広く。**高さが下がっても全面積は常に1**。
- 一様で **区間を広げる** → 密度の高さ $1/(b-a)$ が下がる。「広いほど1点の密度は低い」。
- **ε を 1→0.01** → 区間確率は約 1/100 に（面積≒高さ×幅）。高さは不変。
- **右裾(超過)** に切替 → CDF 図では $1-F(a)$。容量超過＝供給支障確率のイメージ。
- 3つの数値（CDF差・面積・|差|）がほぼ一致 → **「面積＝CDF差」を毎回その場で検証**。

---

## 5. 数学的な対応式

$$F_X(x)=P(X\le x)=\int_{-\infty}^x f_X(t)\,dt,\qquad f_X(x)=\frac{d}{dx}F_X(x)$$
$$P(a\le X\le b)=\int_a^b f_X(x)\,dx = F_X(b)-F_X(a)$$
$$P(X=x)=0,\qquad P(x_0-\varepsilon\le X\le x_0+\varepsilon)\approx f_X(x_0)\cdot 2\varepsilon$$

---

## 6. よくある誤解（アプリ内にも掲載）

| 誤解 | 正しい理解 |
|---|---|
| $f(x)$ は「$x$ が出る確率」 | 密度（高さ）。確率は面積。 |
| 密度は1以下のはず | 1を超えてよい（面積が1）。 |
| $P(X=x)$ は小さいが正 | 連続では厳密に0。 |
| $\le$ と $<$ で確率が違う | 連続では一致（点の確率0）。 |
| PDF と CDF は別物 | 同じ情報。微分・積分で行き来。 |

---

## 7. ミニ演習（アプリ内に解答ヒントあり）

1. $N(100,15)$ で $P(85\le X\le115)$ は？（≈0.68、μ±1σ）
2. 同分布で $P(X>130)$（2σ超過、≈0.023）。容量130万kWの超過確率と解釈。
3. 一様 $U(60,140)$ の密度の高さは？　$U(60,100)$ に狭めると？（0.0125 → 0.025）
4. ε を 1→0.01 にすると区間確率は約何分の1？（≈1/100）

---

## 8. 電力・エネルギーへの接続

- **超過確率** $P(X>\text{容量})$ ＝供給支障・制約違反の確率＝右裾の面積。
- **分位点**（CDF の逆読み）＝「90%の日はこの需要以下」＝設計値・VaR。
- **裾の重さ**：対数正規は右に重い裾。正規で価格・需要を近似すると極端事象を過小評価。
- → Module 6 の**チャンス制約** $P(g(x,\xi)\le 0)\ge 1-\varepsilon$ は、この右裾の面積を $\varepsilon$ 以下に抑える定式化。

---

## 9. ファイル構成

```
apps/pdf_cdf_visualizer/
├── app.py     ← Streamlit UI（Plotly 図、解説エクスパンダ）
├── core.py    ← 数理コア（分布定義・確率計算・自己テスト）。streamlit 非依存
├── SPEC.md    ← 設計仕様（学習設計・受け入れ基準・拡張案）
└── README.md  ← 本ファイル
```
